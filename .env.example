# API 1Doc — Portal de Transparência
ONEDOC_BASE_URL=https://integracoes.***.link
ONEDOC_AUTH_HASH=

# Proteção da rota de diagnóstico /api/sync
# Gerar com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CRON_SECRET=
